# keystroke-accelerometer-ml
Keystroke-labeled 3-axis accelerometer dataset and ML analysis pipeline. An Arduino streams accelerometer readings while a Python logger timestamps keypress events; the notebook aligns both streams to build labeled windows for keystroke classification experiments.
